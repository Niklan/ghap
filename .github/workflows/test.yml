name: Test

on:
  pull_request:
    types:
      - labeled
      - unlabeled
      - closed
      - opened
      - reopened
      - synchronize
  push:
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  test:
    if: startsWith(github.ref, 'refs/tags/')
    name: Test
    runs-on: ubuntu-latest
    steps:
      - name: Test
        shell: bash
        run: echo "Hello, World!"
